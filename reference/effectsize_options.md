# `effectsize` options

Currently, the following global options are supported:

- `es.use_symbols` [logical](https://rdrr.io/r/base/logical.html):
  Should proper symbols be printed (`TRUE`) instead of transliterated
  effect size names (`FALSE`; default).
